describe('selenese test traversal', function() {
    it('reads/parses a server-config file', function() {
    });
    it('reads/parses a test-config file', function() {
    });
    it('reads/parses a testsuite and read selenese files', function() {
    });
    it('reads/parses a selenese file', function() {
    });
    it('execute selenese commands on selenium server', function() {
    });
    it('executes callback, and gets data back from verify-commands', function() {
    });
    it('calls before/after, and adds optional beforeEach/afterEach around other tasks', function() {
    });
    it('replace username/password/server from test-config', function() {
    });
});
describe('command line selenese test', function() {
    it('returns errorcode on error/success', function() {
    });
    it('requires a config file as parameter', function() {
    });
    it('takes user/admin-login/password as command line parameter', function() {
    });
    it('takes test-server/config as parameter', function() {
    });
});
